// Definitions
// Those should be defined like:
// esbuild --define:USE.INTROSPECT=false
export const INTROSPECT = true     // whether we should make code introspectable
// OPTOUT is used for labelling fragments of code to optimize-out